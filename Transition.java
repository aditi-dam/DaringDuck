public class Transition {
    
    public void Transition(Tape inputTape, State nextState, String writeSymbol, String direction) {
        // Tape tape = inputTape;
        // State next = nextState;
        // String write = writeSymbol;
        // String dir = direction; // letter L or R
    }
}

