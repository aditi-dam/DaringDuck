import java.util.*;

public class State {
    
    // State s1 = ...
    // GENERAL, not defining specific states, just defining A STATE

    public void State(ArrayList<Transition> transitions, Boolean isTerminal) {
        ArrayList<Transition> t = transitions;
        Boolean terminal = isTerminal;
    }
    
}
